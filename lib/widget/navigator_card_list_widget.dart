import 'package:flutter/material.dart';

class NavigatorCardList extends StatelessWidget {
  const NavigatorCardList({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView();
  }
}
